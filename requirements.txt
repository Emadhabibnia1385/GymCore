# ============================================================
# GymCore runtime dependencies (bot-first architecture).
# Pinned lower bounds; deployment installs into a dedicated venv.
# ============================================================

# --- Internal backend / service layer (health, optional webhook) ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Database ---
SQLAlchemy>=2.0.30
psycopg[binary]>=3.2 ; python_version >= "3.10"
alembic>=1.13

# --- Validation / settings ---
pydantic>=2.7
pydantic-settings>=2.3

# --- HTTP client (Telegram / Bale Bot API) ---
httpx>=0.27
